#library(sabotag)
#library(sabotagdata)

#data(BEV)
#extent <- extent(BEV[BEV$BL == "Salzburg", ])

#background <- 
#function (extent) {
#	e <- pretty(extent, resolution = "GRID", add = 2)
#	x <- floragrid(e, resolution = "GRID")
#	plot(x)
#	plot(sbg, add = T, col = 2)
#}